import { Canvas, Controls, Meta, Source } from '@storybook/blocks'

import * as HeaderStories from './header.stories'

<Meta of={HeaderStories} />

# Logo

An SVG element representing the Prodkt brand.

## Preview

<Canvas of={HeaderStories.Default} />

<Controls />

## Features

- Full use of SVG properties

## Usage

export const importCode = `import { Menu, MenuItem, HeaderLogo, Header } from '@prodkt/ui/header';`

<Source code={importCode} language='ts' dark />
<Source code={usageCode} language='tsx' dark />

export const usageCode = `<MenuItem />`

<Canvas of={HeaderStories.Default} />
